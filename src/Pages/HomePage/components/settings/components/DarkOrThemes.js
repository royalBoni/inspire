import React from 'react'

const DarkOrThemes = ({handleBack}) => {
  return (
    <div>
      <button className='nav-close-button' onClick={handleBack}>back</button>
      DarkOrThemes
    </div>
  )
}

export default DarkOrThemes